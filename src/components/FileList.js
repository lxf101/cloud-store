import React,{useState, useEffect, useRef} from 'react'
import PropTypes from 'prop-types'

const FileList = ({files,onFileClick, onSaveEdit, onDeleteFile})=>{
    return (
        <ul>
            <li></li>
        </ul>
    )
}

export default FileList;